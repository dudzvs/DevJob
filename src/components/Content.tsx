const Content = () => {
	return (
		<main>
			<SearchBar />
		</main>
	);
};

function SearchBar() {
	return <div></div>; // form for search the job, filtering and submit... maybe pos relative, i dnt know yet try to think about
}

export default Content;
